(()=>{const home=document.querySelector('.os-home');if(!home)return;const key='bom-decision-os-v2',read=()=>{try{return JSON.parse(localStorage.getItem(key)||'{}')}catch{return{}}},write=s=>localStorage.setItem(key,JSON.stringify(s));let state=read();const episode=document.querySelector('.os-episode'),steps=['big','brief','deep','calculator','checklist','action','reflection','outcome','share'];
const render=()=>{const checks=document.querySelectorAll('[data-action-check]:checked').length;document.querySelector('[data-metric="wins"]').textContent=state.outcome?1:0;document.querySelector('[data-metric="episodes"]').textContent=state.outcome?1:0;document.querySelector('[data-metric="confidence"]').textContent=state.confidence===undefined?'—':'Recorded';document.querySelector('[data-metric="actions"]').textContent=checks||state.actions||0};
const reveal=(topic,insight)=>{const r=document.querySelector('[data-win-reveal]');r.hidden=false;r.querySelector('[data-win-topic]').textContent=topic.toUpperCase();r.querySelector('[data-win-insight]').textContent=insight;r.scrollIntoView({behavior:'smooth',block:'center'})};
document.querySelectorAll('[data-decision]').forEach(b=>b.addEventListener('click',()=>reveal(b.dataset.decision,b.dataset.insight)));document.querySelector('[data-situation-form]')?.addEventListener('submit',e=>{e.preventDefault();const v=new FormData(e.currentTarget).get('situation').trim();reveal('YOUR DECISION',v?`A useful next step is to name the decision, the information you need, and the conversation you need to have: “${v}”`:'Start by naming the decision and the next conversation it requires.')});
const show=step=>{episode.hidden=false;document.querySelectorAll('[data-panel]').forEach(p=>p.hidden=p.dataset.panel!==step);document.querySelectorAll('[data-step]').forEach((p,i)=>p.classList.toggle('is-active',p.dataset.step===step)||p.classList.toggle('is-done',i<steps.indexOf(step)));state[step]=true;write(state);render();episode.scrollIntoView({behavior:'smooth',block:'start'})};document.querySelector('[data-open-compensation]')?.addEventListener('click',()=>show('big'));document.querySelectorAll('[data-next]').forEach(b=>b.addEventListener('click',()=>show(b.dataset.next)));
document.querySelectorAll('[data-calc]').forEach(i=>i.addEventListener('input',()=>{const n=k=>Number(document.querySelector(`[data-calc="${k}"]`).value)||0,total=n('guarantee')+Math.max(0,n('units')-n('threshold'))*n('factor');document.querySelector('[data-calculator-result]').textContent=`Illustrative total: ${new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(total)}`}));document.querySelectorAll('[data-action-check]').forEach(c=>c.addEventListener('change',()=>{state.actions=document.querySelectorAll('[data-action-check]:checked').length;write(state);render()}));
const options=document.querySelector('[data-confidence-options]');if(options){options.innerHTML=Array.from({length:11},(_,n)=>`<button type="button" data-confidence="${n}" aria-label="${n} out of 10">${n}</button>`).join('');options.addEventListener('click',e=>{const b=e.target.closest('[data-confidence]');if(!b)return;state.confidence=Number(b.dataset.confidence);write(state);options.querySelectorAll('button').forEach(x=>x.classList.toggle('is-selected',x===b));render()})}document.querySelector('[data-share]')?.addEventListener('click',async()=>{const text='I found this useful: before accepting a compensation offer, ask for the complete written formula and recent reconciliation statements.';try{if(navigator.share)await navigator.share({title:'A compensation decision worth seeing',text,url:location.href});else await navigator.clipboard.writeText(`${text} ${location.href}`);state.outcome=true;write(state);render()}catch{}});render()})();
