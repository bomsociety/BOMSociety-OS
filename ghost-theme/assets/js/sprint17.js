(()=>{const home=document.querySelector('.os-home');if(!home)return;const key='bom-decision-os-v2',read=()=>{try{return JSON.parse(localStorage.getItem(key)||'{}')}catch{return{}}},write=s=>localStorage.setItem(key,JSON.stringify(s));let state=read();const episode=document.querySelector('.os-episode'),steps=['big','brief','deep','calculator','checklist','action','reflection','outcome','share'];
const session=()=>{let id=sessionStorage.getItem('bom-anonymous-session-id');if(!id){id=crypto.randomUUID?.()||`${Date.now()}-${Math.random()}`;sessionStorage.setItem('bom-anonymous-session-id',id)}return id};
const track=(event,detail={})=>window.BOMAnalytics?.track(event,{...detail,anonymous_session_id:session(),timestamp:new Date().toISOString()});
const render=()=>{const checks=document.querySelectorAll('[data-action-check]:checked').length;document.querySelector('[data-metric="wins"]').textContent=state.outcome?1:0;document.querySelector('[data-metric="episodes"]').textContent=state.outcome?1:0;document.querySelector('[data-metric="confidence"]').textContent=state.confidence===undefined?'—':'Recorded';document.querySelector('[data-metric="actions"]').textContent=checks||state.actions||0};
const show=step=>{episode.hidden=false;document.querySelectorAll('[data-panel]').forEach(p=>p.hidden=p.dataset.panel!==step);document.querySelectorAll('[data-step]').forEach((p,i)=>{p.classList.toggle('is-active',p.dataset.step===step);p.classList.toggle('is-done',i<steps.indexOf(step))});state[step]=true;write(state);render();episode.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'start'})};
const startCompensation=(source='hero',context='')=>{if(context)localStorage.setItem('bom-compensation-situation',context);track('compensation_episode_started',{decision_type:'Compensation',availability_status:'available',source_section:source,free_text_present:Boolean(context)});show('big')};
const modal=document.querySelector('[data-request-modal]'), requestTitle=modal?.querySelector('[data-request-title]'), question=modal?.querySelector('[name="question"]');let requestedTopic='';
const openRequest=(topic,context='',source='hero')=>{requestedTopic=topic;requestTitle.textContent=topic;question.value=context;modal.querySelector('[data-request-confirmation]').hidden=true;modal.showModal();setTimeout(()=>question.focus(),0)};
const select=(topic,availability,source,context='')=>{track('homepage_decision_selected',{decision_type:topic,availability_status:availability,source_section:source,free_text_present:Boolean(context)});if(availability==='available')startCompensation(source,context);else openRequest(topic,context,source)};
document.querySelectorAll('[data-decision]').forEach(button=>button.addEventListener('click',()=>select(button.dataset.decision,button.dataset.availability,button.dataset.source)));
document.querySelectorAll('[data-library-decision]').forEach(button=>button.addEventListener('click',()=>{track('decision_library_cta_clicked',{decision_type:button.dataset.libraryDecision,availability_status:button.dataset.availability,source_section:'decision_library'});select(button.dataset.libraryDecision,button.dataset.availability,'decision_library')}));
const mapSituation=value=>{const s=value.toLowerCase();if(/compensation|salary|rvu|wrvu|offer|productivity|bonus/.test(s))return['Compensation','available'];if(/contract|noncompete|non-compete|agreement|terms/.test(s))return['Contract','coming_next'];if(/artificial intelligence|\bai\b|automation/.test(s))return['Artificial Intelligence','coming_next'];if(/ownership|partner|buy.?in|practice/.test(s))return['Practice Ownership','coming_next'];if(/leadership|lead|team|manager/.test(s))return['Leadership','coming_next'];return['A new Decision Episode','coming_next']};
document.querySelector('[data-situation-form]')?.addEventListener('submit',event=>{event.preventDefault();const value=new FormData(event.currentTarget).get('situation').trim();const [topic,availability]=mapSituation(value);track('homepage_situation_submitted',{decision_type:topic,availability_status:availability,source_section:'hero',free_text_present:Boolean(value),mapped_topic:topic,outcome:availability==='available'?'compensation_episode':'request_flow'});select(topic,availability,'natural_language',value)});
modal?.querySelector('[data-request-form]').addEventListener('submit',event=>{event.preventDefault();const hasText=Boolean(question.value.trim());track('unmet_decision_requested',{decision_type:requestedTopic,availability_status:'coming_next',source_section:'request_modal',free_text_present:hasText,mapped_topic:requestedTopic,outcome:'queued_for_consented_data_path'});const confirmation=modal.querySelector('[data-request-confirmation]');confirmation.textContent=`Thank you. ${requestedTopic} is still in development; your anonymous demand signal is prepared for the future intelligence layer.`;confirmation.hidden=false;question.disabled=true;event.currentTarget.querySelector('[type="submit"]').disabled=true});
modal?.addEventListener('close',()=>{question.disabled=false;modal.querySelector('[type="submit"]').disabled=false});
document.querySelectorAll('[data-next]').forEach(b=>b.addEventListener('click',()=>show(b.dataset.next)));document.querySelectorAll('[data-calc]').forEach(i=>i.addEventListener('input',()=>{const n=k=>Number(document.querySelector(`[data-calc="${k}"]`).value)||0,total=n('guarantee')+Math.max(0,n('units')-n('threshold'))*n('factor');document.querySelector('[data-calculator-result]').textContent=`Illustrative total: ${new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(total)}`}));document.querySelectorAll('[data-action-check]').forEach(c=>c.addEventListener('change',()=>{state.actions=document.querySelectorAll('[data-action-check]:checked').length;write(state);render()}));
const options=document.querySelector('[data-confidence-options]');if(options){options.innerHTML=Array.from({length:11},(_,n)=>`<button type="button" data-confidence="${n}" aria-label="${n} out of 10">${n}</button>`).join('');options.addEventListener('click',e=>{const b=e.target.closest('[data-confidence]');if(!b)return;state.confidence=Number(b.dataset.confidence);write(state);options.querySelectorAll('button').forEach(x=>x.classList.toggle('is-selected',x===b));render()})}const copy=async value=>{if(navigator.clipboard?.writeText)return navigator.clipboard.writeText(value);const field=document.createElement('textarea');field.value=value;field.style.position='fixed';field.style.opacity='0';document.body.append(field);field.select();document.execCommand('copy');field.remove()};document.querySelectorAll('[data-share]').forEach(button=>button.addEventListener('click',async()=>{const text='I found a clear explanation of how physician compensation formulas actually work. This may be useful before your next contract or compensation discussion.';track('colleague_share_started',{decision_type:'Compensation',source_section:'decision_library'});try{if(navigator.share)await navigator.share({title:'Physician compensation formulas',text,url:location.href});else await copy(`${text} ${location.href}`);track('colleague_share_completed',{decision_type:'Compensation',source_section:'decision_library',outcome:'shared'});state.outcome=true;write(state);render();button.textContent='Link ready to share';}catch{}}));render()})();
